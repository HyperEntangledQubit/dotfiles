- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vim:
    ~/.vimrc:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.lcovrc:

- create:
    - ~/Work
    - ~/.vim/undo-history

- shell:
  - [git submodule update --init --recursive, Installing submodules]

