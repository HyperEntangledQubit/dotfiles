- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vim:
    ~/.vimrc:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.lcovrc:
    ~/.cargo:

- create:
    - ~/Work
    - ~/.vim/undo-history

- shell:
  - [git submodule update --init --recursive, Installing submodules]

